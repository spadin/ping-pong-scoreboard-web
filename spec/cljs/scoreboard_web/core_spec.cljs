(ns scoreboard-web.core-spec
  (:require-macros [specljs.core :refer [describe it should=]])
  (:require [specljs.core]
            [scoreboard-web.core]))

(describe "A ClojureScript test"
  (it "fails. Fix it!"
    (should= 1 1)))
